# Wind Window Operator AI - Dependencies

# Claude API (required)
anthropic>=0.40.0

# Screen control / execution engine (required for AI to actually operate)
pyautogui>=0.9.54       # Mouse, keyboard, screenshot control
pygetwindow>=0.0.9      # Window focus/management

# Screen capture (choose one or both)
mss>=9.0.1              # Fast cross-platform screen capture
Pillow>=10.0.0          # Image handling and save

# Screen recording (optional, for recording mode)
opencv-python>=4.9.0    # Video writing
numpy>=1.24.0           # Array processing for video frames

# Text-to-speech (optional, for voice recommendations)
# Option 1: Offline TTS (simpler setup)
pyttsx3>=2.90           # Offline TTS, works on Windows/Mac/Linux
# Option 2: Online TTS with better Japanese support
# gtts>=2.5.0           # Google TTS (requires internet)
# playsound>=1.3.0      # Play audio files (for gTTS)

# GUI (built into Python, no install needed)
# tkinter - comes with Python standard library
